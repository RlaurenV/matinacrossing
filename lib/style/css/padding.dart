import 'package:flutter/material.dart';

class PaddingStyle extends StatelessWidget {
  const PaddingStyle({super.key});

  @override
  Widget build(context) {
    return const Padding(
      padding: EdgeInsets.all(10.0),
    );
  }
}
