import 'package:flutter/material.dart';

class FqaPageContent extends StatelessWidget {
  const FqaPageContent({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('test'),
    );
  }
}
