import 'package:flutter/material.dart';

class GeneralInformationPageContent extends StatelessWidget {
  const GeneralInformationPageContent({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('test'),
    );
  }
}
